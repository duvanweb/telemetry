// ExitScreen is a placeholder that is never rendered. The Exit tab's custom
// tabBarButton (defined in _layout.tsx) intercepts the press and performs the
// exit action directly — clearing the vehicle and navigating to /setup.
export default function ExitScreen() {
  return null;
}
